Actúa como un Arquitecto de Software Senior y experto en refactorización, 
aplicando estrictamente los principios SOLID y las reglas de Clean Code de Robert C. Martin (Uncle Bob) y Martin Fowler.

Tu tarea es refactorizar la clase de Java que te proporciono al final de este mensaje. 

He analizado el código y he detectado varios "Code Smells" críticos que debes solucionar:
1. Violación del Principio de Responsabilidad Única (SRP): El método hace demasiadas cosas (calcula descuentos, conecta a base de datos y envía correos).
2. Alto Acoplamiento: Dependencia directa de implementaciones concretas como la conexión a base de datos y servicios estáticos de email.
3. Números Mágicos (Magic Numbers): Porcentajes de descuento y tipos de orden hardcodeados en el código.
4. Nombres poco descriptivos: Nombres de variables y métodos incomprensibles como 'p', 'ord', 'd' o 'amt'.

Por favor, reescribe el código aplicando las siguientes directrices:
- Nombres significativos.
- Extracción de interfaces.
- Elimina los bloques try-catch genéricos y maneja las excepciones de forma más limpia o delégalas si es necesario.

Devuélveme el código de la clase principal refactorizada y las interfaces auxiliares necesarias.

Aquí tienes el código original:

public class OrderManager {

    // Método que procesa una orden, aplica descuentos, guarda en BD y notifica.
    public boolean p(Order ord) {
        // 1. Lógica de negocio mezclada
        if (ord.type == 1) { 
            double d = ord.amt * 0.15; 
            ord.amt = ord.amt - d;
        } else if (ord.type == 2) {
            double d = ord.amt * 0.05;
            ord.amt = ord.amt - d;
        }

        // 2. Acoplamiento fuerte con Base de Datos
        try {
            DBConnection db = new DBConnection("jdbc:mysql://localhost:3306/shop", "user", "1234");
            db.execute("UPDATE orders SET total = " + ord.amt + " WHERE id = " + ord.id);
        } catch (Exception e) {
            System.out.println("Error");
            return false;
        }

        // 3. Efectos secundarios (envío de email)
        EmailService.send(ord.email, "Su pedido de " + ord.amt + " ha sido procesado");
        return true;
    }
}